# Polyglot Document

Some JavaScript:

```js
const x = () => 42;
```

Then some Python:

```python
x = lambda: 42
```

And shell:

```bash
echo $((6 * 7))
```

Inline `code` and a $math$ symbol. Done.

```json
{ "answer": 42 }
```
