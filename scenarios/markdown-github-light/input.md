# shikiphp

A **pure-PHP** port of [Shiki](https://shiki.style).

## Features

- TextMate grammars
- VS Code themes
- `codeToHtml()` and `codeToTokens()`

> Note: highlighting is compared against Shiki.js.

```js
const x = highlight('code');
```

1. Load grammar
2. Tokenize
3. Render
