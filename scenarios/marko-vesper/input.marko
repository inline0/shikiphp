class {
  onCreate() {
    this.state = { count: 0 };
  }
  increment() {
    this.state.count++;
  }
}

<div class="counter">
  <p>Count: ${state.count}</p>
  <button on-click("increment")>
    Click me
  </button>
  <for|item| of=input.items>
    <li>${item.name}</li>
  </for>
</div>
