name = "my-app"
version = "1.0.0"

server {
  host = "0.0.0.0"
  port = 8080
  timeout = 30.s
}

endpoints {
  ["health"] {
    path = "/health"
    method = "GET"
  }
}

replicas = if (version == "1.0.0") 3 else 1
